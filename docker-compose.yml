services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - prezio_data:/app/data
    restart: unless-stopped

volumes:
  prezio_data: